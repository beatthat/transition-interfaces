{
  "name": "unity-transition-interfaces",
  "version": "0.0.2",
  "description": "Transitions are behaviours that start and then run to completion with an option to cancel. An example is something like a FadeIn for a UI panel. The standalone interfaces package allows other packages to interact with transitions without a dependency on any implementation.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beatthat/unity-transition-interfaces.git"
  },
  "files": [
    "index.js",
    "scripts",
    "src"
  ],
  "author": "larrykirschner@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/beatthat/unity-transition-interfaces/issues"
  },
  "homepage": "https://github.com/beatthat/unity-transition-interfaces",
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "install:test": "rm -f *.tgz && npm pack && cd test && npm run clean && npm install"
  },
  "dependencies": {
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "unity-request-interfaces": "github:beatthat/unity-request-interfaces#semver:^0.0.1"
  }
}
